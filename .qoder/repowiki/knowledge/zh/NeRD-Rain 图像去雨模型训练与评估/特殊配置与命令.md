- **环境配置**：需手动安装预热调度器：`cd pytorch-gradual-warmup-lr; python setup.py install`。
- **训练启动**：使用 `bash train.sh` 或 `python train.py`，支持通过 argparse 指定数据集路径、patch 大小及会话名称。
- **评估方式**：Python 生成去雨结果后，需调用 `evaluations/` 下的 `.m` (MATLAB) 脚本计算最终指标。