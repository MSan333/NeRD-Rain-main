- 模型组件（如 LayerNorm, Attention, FeedForward）均定义为继承 nn.Module 的独立类，并在 TransformerBlock 中组合。
- 损失函数采用多任务加权组合，包括空域（Charbonnier, L1）、频域（FFT）和边缘（Edge）损失，以约束多尺度输出。
- 数据增强在 DataLoader 中实现，包含随机裁剪、翻转、旋转以及亮度和饱和度的动态调整。
- 跨尺度交互通过显式的 Upsample/Downsample 配合 Fusion 模块完成，而非简单的 skip connection。